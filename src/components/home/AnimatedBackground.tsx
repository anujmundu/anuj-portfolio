import React from 'react';

// AnimatedBackground – a lightweight gradient animation for the SelectedWork section.
// The component renders a full-size, absolutely positioned <div> that animates
// a background gradient using CSS keyframes defined in selectedWork.css.

export default function AnimatedBackground() {
  return <div className="animated-bg" />;
}
